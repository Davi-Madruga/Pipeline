name: Validação HTML

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  verificar-forms:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do Código
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Instalar o HTML-Validate
        run: npm install -g html-validate

      - name: Validar forms.html
        # Este job verificará o forms.html
        run: |
          echo "🔍 Validando forms.html..."
          html-validate forms.html
          
  verificar-index: # Início do segundo job
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do Código
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Instalar o HTML-Validate
        run: npm install -g html-validate

      - name: Validar index.html
        run: |
          echo "🔍 Validando index.html..." # Mensagem corrigida
          html-validate index.html        # Comando corrigido