name: Validação HTML

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  verificar-forms:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do Código
        uses: actions/checkout@v4

      - name: Validar forms.html
        # 🚨 CORRIGIDO: Usando o nome correto da Action oficial
        uses: html-validate/html-validate-action@v1
        with:
          args: --files forms.html
          # A regra "no-unknown-elements" é essencial para falhar nas tags <sa> e <sasa>
          rules: 'doctype-first, tag-pair, no-unknown-elements'

  verificar-index:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do Código
        uses: actions/checkout@v4

      - name: Validar index.html
        # 🚨 CORRIGIDO: Usando o nome correto da Action oficial
        uses: html-validate/html-validate-action@v1
        with:
          args: --files index.html
          rules: 'doctype-first, tag-pair, no-unknown-elements'