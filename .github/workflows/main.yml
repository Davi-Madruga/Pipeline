name: Validação HTML

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  validar-html:
    runs-on: ubuntu-latest

    steps:
      - name: Fazer checkout do repositório
        uses: actions/checkout@v4

      - name: Instalar Node.js (necessário pro htmlhint)
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Instalar htmlhint
        run: npm install -g htmlhint

      - name: Rodar verificação de HTML
        run: |
          echo "🔍 Verificando tags HTML..."
          htmlhint "**/*.html"
